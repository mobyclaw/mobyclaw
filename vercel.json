{
  "version": 2,
  "buildCommand": "pip install mkdocs-material && mkdocs build --config-file mkdocs.yml --site-dir _site/docs && mkdir -p _site && cp site/index.html _site/index.html",
  "outputDirectory": "_site",
  "installCommand": "echo 'no npm install needed'",
  "framework": null,
  "routes": [
    {
      "src": "/docs/(.*)",
      "dest": "/docs/$1"
    },
    {
      "src": "/",
      "dest": "/index.html"
    }
  ]
}
